# Hoist for Next.js / Metro compatibility in mixed monorepos
shamefully-hoist=true
strict-peer-dependencies=false
# EAS runs pnpm with --frozen-lockfile (SDK 52 + RN 0.76+); relax strict checks on CI
engine-strict=false
package-manager-strict=false
