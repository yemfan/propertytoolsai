// @helm/dna-knowledge — Knowledge DNA: knowledge store + vector search + templates + prompts.
// Core package. MUST NOT import @helm/pack-* or apps/*. (enforced by scripts/check-boundaries.mjs)
// First extraction: org-scoped client-note capture.
export * from "./notes";
