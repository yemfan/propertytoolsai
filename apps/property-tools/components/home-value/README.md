# Home value UI (`components/home-value/`)

Shared sections and compliance chrome used by **`components/homeValue/HomeValueTool.tsx`** (main funnel).

| File | Role |
|------|------|
| `HomeValueSection.tsx` | Numbered section shell (headings + `scroll-mt` anchors) |
| `HomeValueTrustDisclaimer.tsx` | “Informational only / not an appraisal” + range note |

The full layout implements **sections 1–7** in the tool: hero & address, estimate preview, refine, report gate, detailed report, recommendations / next steps, expert help CTA.
