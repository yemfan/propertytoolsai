import type { ReactNode } from "react";

export const metadata = {
  title: "Layout preview — PropertyTools AI",
  robots: { index: false, follow: false },
};

export default function LayoutPreviewLayout({ children }: { children: ReactNode }) {
  return children;
}
