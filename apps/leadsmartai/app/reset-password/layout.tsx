export const metadata = {
  title: "Set new password | LeadSmart AI",
  description: "Choose a new password for your LeadSmart AI account.",
};

export default function ResetPasswordLayout({ children }: { children: React.ReactNode }) {
  return children;
}
