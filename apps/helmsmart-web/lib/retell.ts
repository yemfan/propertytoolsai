// The Retell REST client moved to the shared @repo/voice package (used by every
// app that runs the voice agent). Re-exported here so existing `@/lib/retell`
// imports keep working.
export * from "@repo/voice/retell";
